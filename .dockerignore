node_modules
npm-debug.log
.env
data/db.json
# Демо-сид с фейковыми магазинами и демо-токенами (admin-demo-token и пр.) — нужен
# только при SEED_DEMO=yes (локалка), в прод-образ (Neon) не тащим.
data/sample-db.json
# Диагностические снимки 1С — write-only кэш, в рантайме не читаются; 0.5+ МБ балласта.
data/upp-snapshots/
# Рантайм-кэш расчётов 1С (генерится на месте, переживает рестарты) — не печём в образ.
data/cache/
# Списки SMS-получателей с ПДн (ФИО+телефоны) — на всякий случай и из образа тоже.
data/sms-*.txt
data/sms-*.csv
