services:
  - type: web
    name: sales-plan-dashboard
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 3000
      - key: INGEST_API_KEY
        generateValue: true
      - key: DATABASE_URL
        sync: false
